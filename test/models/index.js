const user = require('./user')
const game = require('./user')
const userGame = require('./user')
const userPrefer = require('./userPrefer')

module.exports = {
    user,
    userPrefer,
    game,
    userGame
}